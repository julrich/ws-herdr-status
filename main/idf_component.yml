dependencies:
  idf: ">=5.5.0"
  # Pinned to the versions Waveshare validates this board against (see the
  # dependencies.lock shipped in their 03_lvgl_example).
  lvgl/lvgl: "^9.0.0"
  espressif/esp_lcd_touch: "1.1.2"
  espressif/esp_lvgl_port: "2.5.0"
